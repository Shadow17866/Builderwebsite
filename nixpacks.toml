[phases.setup]
nixPkgs = ['python39', 'pip']

[phases.install]
cmds = ['cd FloorPlanTo3D-API-master && pip install --upgrade pip && pip install -r requirements.txt']

[phases.build]
cmds = []

[start]
cmd = 'cd FloorPlanTo3D-API-master && python application.py'
